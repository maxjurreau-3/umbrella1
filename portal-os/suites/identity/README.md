# Identity Suite

The Identity Suite handles authentication, user profiles, and credential management.

## Components

- **api/**: Authentication endpoints, user profiles, credentials API
- **ui/**: Login forms, account settings, credential UI

## Dependencies

- Governance.IDENTITY (identity graph)
- Portal-OS.KERNEL (IPC for events)
- Portal-OS.RUNTIME (routing)
