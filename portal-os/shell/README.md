# SHELL - Web Interface

SHELL provides the web interface for Portal-OS:

- **web/**: HTTP server, middleware, request handling
- **components/**: UI component library
- **assets/**: Static files, stylesheets, images

## Responsibilities

SHELL depends on RUNTIME and serves the user interface. It handles HTTP requests, renders UI components, and serves static assets.
